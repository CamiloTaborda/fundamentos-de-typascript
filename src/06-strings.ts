(() => {
let productTitle = 'Product Title';
productTitle = 'New Product Title';
console.log(productTitle);

const producDescription = "jnsj jbdjb jbdb";
console.log(producDescription);

const summary =  `
title: ${productTitle}
description: ${producDescription}
`;
console.log(summary);

const myString: string = '';
})();
